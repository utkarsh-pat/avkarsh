export default function Loading() {
  return (
    <main className="setup-shell" aria-busy="true">
      <section className="setup-card">
        <p className="eyebrow">LOADING PROPERTY ACCESS</p>
        <h1>Preparing your workspace.</h1>
      </section>
    </main>
  );
}
